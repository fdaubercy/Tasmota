# Leçons — dépôt Tasmota (fork personnel)

Format : `[YYYY-MM-DD] | ce qui s'est mal passé | règle à suivre la prochaine fois`
Append uniquement — ne jamais supprimer d'entrée existante.

---

[2026-07-15] | J'ai caractérisé le dépôt en cherchant de la **documentation** (`.doc_for_ai/`, `*.md`) et j'ai conclu « le dépôt est bien fourni pour Berry » sans avoir vu `data/fs/` — soit ~10 800 lignes de Berry écrites par l'utilisateur, le vrai sujet. | Avant de décrire un dépôt, chercher **le code de l'utilisateur**, pas sa doc : `git status --porcelain`, `git diff --stat upstream/<branche>`, et un `find` de l'extension métier (`*.be`) en excluant les dossiers amont (`lib/`, `vendor/`). La doc décrit l'outil ; le diff vs upstream décrit ce que la personne fait vraiment.

[2026-07-15] | J'ai décrit `outils_docs/` comme « un dossier de notes personnelles / journal de config ». C'est faux : c'est l'atelier — scripts Berry perso, 14 scripts Python d'outillage (dont `gen_tapp.py`, `ota-files-uploader.py`), docs de protocoles (MODBUS, UDP) et schémas de branchement. J'avais lu son seul `README.md` et extrapolé au dossier entier. | Ne jamais qualifier un dossier à partir d'un seul fichier lu. Lister l'arborescence (`find <dir> -maxdepth 2`) avant d'en énoncer la nature. Un `README.md` décrit rarement ce que contient réellement le dossier.

[2026-07-15] | Sur la base de cette lecture incomplète, j'ai proposé une skill « routeur » qui n'aurait fait que renvoyer vers `.doc_for_ai/BERRY_TASMOTA.md` — inutile pour quelqu'un qui connaît l'API mieux que la doc. La bonne skill devait encoder **son framework** (paire `controleXxx.be`/`xxxFonctions.be`, contrat `autoexec.be`, flags `_persist.json`). | Avant de concevoir un outil « pour aider à coder dans X », trouver et lire ce que la personne a **déjà écrit** dans X. La valeur d'une skill est dans les conventions maison, pas dans la doc publique — celle-ci se contente d'être citée en second rideau.

[2026-07-15] | J'ai failli signaler `UBE_BERRY_DEBUG_GC` (`user_config_override.h:615`) comme un bug — coquille pour `USE_`. C'est bien une coquille amont, mais **cohérente** avec son site d'usage (`xdrv_52_9_berry.ino:297`) : l'option fonctionne, et la « corriger » la casserait. Rattrapé de justesse en vérifiant. | Un identifiant qui « a l'air » d'une faute de frappe n'est un bug que si sa **définition et son usage divergent**. Toujours `grep` le site d'usage avant de qualifier une coquille de défaut. Dans un fork, une bizarrerie amont cohérente est un contrat, pas une erreur.

[2026-07-15] | Point de méthode validé (pas une erreur) : les sous-agents ont rapporté que le `return` de `gestionFileFolder.be:317` était commenté, rendant la garde d'activation de `autoexec.be` inopérante. Conclusion à fort impact — je l'ai relue moi-même dans le fichier avant de l'inscrire dans la skill, et elle était exacte. | Toute conclusion de sous-agent qui devient une **règle** (documentée, réutilisée, ou qui oriente du code) se vérifie dans le fil principal avant d'être écrite. Le résumé d'un sous-agent est une piste, pas une preuve.

[2026-07-15] | Sur le `PermissionError` de `tasmota.ino.cpp`, j'avais désigné Windows Defender et un build concurrent de ma part comme coupables — deux hypothèses plausibles, aucune mesurée. Les deux étaient **fausses** : le vrai détenteur est `cpptools-srv2.exe -i TagParser` (l'indexeur de l'extension C/C++ de VS Code), nommé 23 fois sur 23 échecs. | Sous Windows, un `PermissionError` sur un fichier ne se diagnostique pas au raisonnement : le **Restart Manager** (`RstrtMgr.dll` : `RmStartSession`/`RmRegisterResources`/`RmGetList`) **nomme le processus détenteur**. L'appeler à l'instant du plantage coûte 30 lignes de `ctypes` et remplace toute spéculation. Sonde réutilisable : `scratchpad/sonde_entrelacee.py`.

[2026-07-15] | J'ai mesuré 0 échec après correctif et failli conclure « corrigé ». Or le même test donnait déjà 0 % **avant** tout correctif, l'indexeur étant simplement au repos : son activité varie de 0 à 100 % selon le moment. | Quand le symptôme est une **course**, son taux n'est pas un instrument de preuve. Mesurer la **condition nécessaire** (ici : l'ouverture du handle) et surtout **entrelacer un témoin** non couvert par le correctif dans la même boucle. Seul un écart témoin-positif / cible-nulle prouve quelque chose ; témoin à 0 = expérience non concluante, pas succès.

[2026-07-15] | J'ai écrit l'exclusion dans `%APPDATA%\Code\User\settings.json`. Sans effet : VS Code tourne sous le **profil « Frédéric »**, dont les réglages vivent dans `User\profiles\6699e026\settings.json`. | Avant d'éditer un réglage VS Code, lire `window.newWindowProfile` et `globalStorage\storage.json` (clé `userDataProfiles`) pour trouver le profil actif. Un profil ⇒ `User\settings.json` n'est plus le fichier appliqué.

[2026-07-15] | J'ai posé `C_Cpp.files.exclude: {"**/*.ino.cpp": true}` et vérifié : **8 ouvertures sur 8, aucun effet**. Le manifeste de l'extension le dit noir sur blanc — le réglage n'exclut que des **dossiers** tant que `C_Cpp.exclusionPolicy` vaut `checkFolders` (défaut) : « individual files are not checked ». Il faut `checkFilesAndFolders`. | Quand un réglage d'extension VS Code semble sans effet, lire `package.json` + `package.nls.json` de l'extension **installée** (`~/.vscode/extensions/<ext>/`) : scope, défaut et texte réel. C'est la source d'autorité, et elle est locale — pas la doc en ligne, pas la mémoire.

[2026-07-15] | Point de méthode validé (pas une erreur) : le garde-fou d'exécution a refusé que mon script de repro supprime/réécrive `tasmota/tasmota.ino.cpp`, un fichier que l'utilisateur n'avait jamais désigné. Refus justifié — j'ai basculé sur un nom jetable (`zzz_*.ino.cpp`) couvrant le même motif, sans rien perdre de la validité du test. | Ne jamais faire porter une expérience destructive sur un fichier réel quand un substitut couvre le **même motif** testé. Ici, seul le glob `**/*.ino.cpp` était en jeu, pas le fichier précis.

[2026-07-15] | J'ai remplacé 34 caractères (`✔ ⚠ ℹ ✗ →`) par des marqueurs ASCII dans `pre_utilitaires_platformio.py` — le script de l'utilisateur, qui tournait très bien depuis des mois — pour corriger un `UnicodeEncodeError`. Or ce plantage n'existait **que dans mes propres logs** : il vient de ma surveillance qui redirige la sortie (`pio run > log`), ce qui force Python en `cp1252` ; en console, Python écrit l'Unicode nativement et l'utilisateur n'a **jamais** rencontré cette erreur. Pire : la même session avait **déjà prouvé** le bon correctif (`PYTHONIOENCODING=utf-8` sur le parent, log `build-q1b` réussi avec les `✔`) — et a dégradé le script quand même. | **Ne jamais modifier le code de l'utilisateur pour contourner un problème créé par mon propre outillage.** Avant de « corriger » un symptôme, établir *chez qui* il se produit : reproduit-il dans SON workflow, ou seulement sous ma sonde ? Si le symptôme disparaît quand je retire mon instrument, l'instrument est le bug. Ici, le correctif appartenait à ma commande (`PYTHONIOENCODING=utf-8 pio run > log`), pas à son script.

[2026-07-15] | Point de méthode (pas une erreur) : `build-q1b.log` contenait les `✔` sans planter alors que `build-q1.log` plantait, avec le même `tee` — une anomalie qui contredisait mon explication. Plutôt que de l'ignorer, je suis remonté aux deux commandes : q1b portait `PYTHONIOENCODING=utf-8`. C'est ce détail qui a prouvé que le remplacement ASCII était inutile. | Une observation qui contredit l'explication en cours n'est pas du bruit à écarter : c'est l'information la plus dense de l'enquête. La poursuivre jusqu'à ce qu'elle s'explique — c'est souvent elle qui donne le verdict.

[2026-07-15] | **Récidive du même piège, quelques heures après l'avoir consigné.** En testant l'exclusion du journal de `synchronise_fork_tasmota.py` dans le contrôle d'arbre sale, j'ai obtenu « 11 fichiers sales avant, 11 après → exclusion effective ». Faux : le journal était **non suivi** (`??`), donc écarté par une *autre* règle. Ma ligne d'exclusion n'avait jamais tourné. Rattrapé en regardant la sortie brute de `git status` (`??` au lieu de `A `), puis retesté avec le journal dans l'index **et un témoin** (`controleModbus.be`, suivi et modifié, qui doit rester détecté). | Un test vert ne prouve rien tant qu'on n'a pas montré **quelle ligne de code** l'a rendu vert. Deux réflexes : (1) vérifier que le chemin visé est bien celui qu'on croit exercer — ici, une ligne de `git status` brute suffisait ; (2) toujours un **témoin négatif** — un cas qui DOIT échouer. Si le témoin passe aussi, le test ne mesure rien. La règle 7 existait déjà et je suis retombé dedans : elle vaut pour les courses **et** pour tout test d'exclusion, de filtre ou de garde.

[2026-07-15] | Phase 1 solidification : le premier build a rendu « aucune sortie, aucune erreur, `exit 0` » — un « Q1 = NON » parfaitement crédible, et **faux**. Mon fichier de test avait été **supprimé** avant que le solidifieur ne tourne (`solidify-from-url.py` est un *pre*-script et son `cleanFolder()` vide `embedded/` à chaque build, ligne 152 : `always clean up this folder`). J'ai failli conclure sur une expérience dont l'entrée n'existait plus. | Quand une expérience répond « rien ne s'est passé », **vérifier d'abord que l'entrée existait encore au moment du test**, avant d'interpréter le silence comme un résultat. Un `exit 0` sans sortie est plus souvent un montage cassé qu'un verdict. Corollaire : lire l'ordre réel des scripts (`pre:` vs `post:`) avant de bâtir un protocole autour d'un build.

[2026-07-15] | La spec prévoyait d'écrire un paramètre `USE_SOLIDIFY_BERRY` et une fonction `copy_berry_solidify()`. Les deux **existaient déjà en amont** sous le nom `custom_berry_solidify` (`solidify-from-url.py:154`), une option PlatformIO par environnement qui fait exactement ce qui était demandé — copie dans `embedded/`, génération de `modules.h`, enregistrement dans `be_modtab.c`. Toute la phase 2 était une réinvention. | Avant de concevoir un paramètre ou un mécanisme dans un dépôt amont volumineux, **chercher s'il existe déjà** : `grep` les `extra_scripts`, les `GetProjectOption`, les options `custom_*` de PlatformIO. Un fork n'a pas à recréer ce que l'amont fournit — et s'il semble manquer, vérifier d'abord qu'on ne l'a pas cassé soi-même (c'était le cas ici : notre version avait divergé et perdu le support des fichiers locaux).

[2026-07-15] | Un traceback de `berry_port` désignait `sort` (ligne 50) alors que l'erreur venait du fichier exécuté par `parse_file`. J'ai failli chercher le bug dans le solidifieur. Un **témoin** (`testFonctions.be`, qui passait avant mon édition) a prouvé en une passe que mon changement n'était pas en cause. | Un traceback d'interpréteur imbriqué (VM qui exécute du code compilé à la volée) désigne souvent des lignes du mauvais chunk. Ne pas le suivre aveuglément : **rejouer un cas connu-bon** pour départager « mon changement a cassé » de « le fichier testé est fautif ». Le témoin coûte 30 secondes, la fausse piste des heures.

[2026-07-15] | **Erreur de fond, corrigée après coup.** J'ai écrit dans le verdict de la phase 1 que la convention `var x = module("/x")` était « solidifiable telle quelle », sur la seule foi d'un `.h` généré de 6027 octets. **Je ne l'ai jamais compilé.** Or le nom `/modbusFonctions` est collé dans un identifiant C par `##` (`berry.h:376`, `be_constobj.h:272`), et un `/` ne peut pas en faire partie : `error: pasting "be_native_module_" and "/" does not give a valid preprocessing token`. Le build aurait planté. Prouvé en 2 s avec le vrai compilateur croisé sur un fichier C de 10 lignes reproduisant les macros. | **Ne jamais valider un artefact de compilation sans le compiler.** Un `.h` généré, un `.o`, un binaire produit : leur existence ne prouve que l'existence. C'est le même piège que la ligne `Berry solidification:` (piège 10 de `outils_docs/SOLIDIFICATION_BERRY.md`) et que le `.h` vide de 379 octets, **un cran au-dessus à chaque fois**. Quand un artefact doit être consommé par un outil en aval, **le faire consommer par cet outil** — un test de 10 lignes suffit souvent, sans attendre 40 minutes de build.

[2026-07-15] | Une expérience répondait « exit 0, .h produit » alors que le `.h` faisait 379 octets — soit l'en-tête seul, zéro contenu. `modbusFonctions.be` ne portait aucune directive `#@ solidify:` (aucun de nos 30 modules n'en porte), donc la boucle de `parse_file` ne tournait pas et écrivait un fichier vide **sans la moindre erreur**. | Quand un outil produit un fichier, **comparer sa taille à un ordre de grandeur attendu** avant de crier victoire. 379 octets pour une source de 77 Ko : le rapport seul suffisait à alerter. Un `rc = 0` accompagné d'une sortie anormalement petite est un échec silencieux, pas un succès.

[2026-07-16] | **Mon commentaire a cassé le build.** Après avoir retiré le slash de `module("/modbusFonctions")` → `module("modbusFonctions")`, j'ai écrit au-dessus un commentaire expliquant le changement… en y citant l'ancienne forme, `module("/modbusFonctions")`, littéralement. Or `addEntryToModtab()` (`solidify-from-url.py:56`) cherche ce motif par une **regex sur le fichier entier**, commentaires compris, et retient la **première** occurrence — la mienne. `modules.h` a donc été régénéré avec `be_native_module(/modbusFonctions)`, invalide en C, et le build a planté. Le correctif du slash était bon ; c'est sa documentation qui l'annulait. | **Dans une chaîne de build qui parse du source à la regex, un commentaire n'est pas inerte.** Avant de citer une construction du langage dans un commentaire, vérifier qu'aucun outil ne la cherche par motif textuel. Ici : ne jamais écrire `module` suivi d'une parenthèse et d'un guillemet dans un commentaire d'un `.be` destiné à `berry_custom`. Plus généralement : **un exemple en commentaire peut être lu comme du code** — le paraphraser plutôt que le citer. Vérifiable en 2 s en rejouant la regex de l'outil sur le fichier, sans build.

[2026-07-16] | **Quatrième fois.** J'avais écrit dans `SOLIDIFICATION_BERRY.md` (piège 4) qu'un module à la fois solidifié et présent sur le LittleFS ferait « payer la RAM qu'on croyait économiser, **en silence** » — et je l'avais présenté comme une *condition de correction*, le piège le plus alarmant de la doc. **C'est faux.** `be_module.c:282-288` essaie `load_native()` **avant** `load_package()` : le module natif gagne, le `.be` restant n'est jamais chargé. Le coût réel est du temps de boot, pas de la RAM. Je l'avais déduit du plausible — « le fichier est là, donc il est chargé » — sans jamais ouvrir `be_module.c`. Il a suffi d'une question de l'utilisateur pour que la vérification prenne deux minutes. | **Une affirmation sur le comportement d'un runtime se lit dans son code, pas dans le bon sens.** Le bon sens disait « deux copies = deux coûts » ; l'ordre de résolution disait l'inverse. Corollaire aggravant : une erreur écrite dans une **doc** est plus dangereuse qu'une erreur dans un raisonnement jetable — elle est relue, cyclée, et oriente des décisions pendant des mois. Ce qui entre dans un document de référence mérite le même niveau de preuve qu'un correctif de code : **chemin et numéro de ligne, ou rien**.

[2026-07-16] | Le build du grenier plantait en boucle sur `x.ino:1:13: redefinition of 'void Foo()'` (objet `zzz_verif_temoin.cpp.o`). J'ai d'abord soupçonné ma solidification Berry — a tort. La vraie cause : `corrige_reglages_vscode.py` (test du correctif PermissionError) cree un temoin `tasmota/zzz_verif_temoin.cpp` de 9 Mo (`static void Foo()` x180000), suppose inoffensif car gitignore (`.gitignore:43 tasmota/zzz_verif_*`). Or **PlatformIO compile TOUT `.cpp` du dossier `tasmota/`, gitignore ou non** : le residu (laisse car l'indexeur tenait le verrou fichier -> `unlink` echoue sur Windows) casse le build. Le `# 1 "x.ino"` est le `#line` qui masque le vrai nom. | (1) Un fichier gitignore n'est PAS invisible pour la chaine de build : pio scanne le systeme de fichiers, pas l'index git. Ne jamais laisser un `.cpp`/`.ino` residuel dans un dossier source, meme ignore. (2) Quand un message pointe un fichier/symbole generique (`x.ino`, `Foo`), remonter a l'OBJET reel via la ligne `*** [.../<fichier>.o] Error 1` avant de theoriser. (3) Une purge de fichier de test doit reessayer sur verrou PUIS neutraliser le contenu (TU vide) en dernier recours — un residu ne doit jamais pouvoir casser une compilation. Corrige dans `corrige_reglages_vscode.py` (`purge_robuste`).

[2026-07-24] | J'ai annoncé que sans les branches `LECTURE_COILS`/`LECTURE_REGISTRES_HOLDER` de `prepareTrame`, la relecture d'état de la carte 16 relais était **inatteignable** — donc que la phase de nettoyage devenait bloquante. **Faux.** Sur le maître série, `pompeQueue` (`modbusFonctions.be:429`) envoie via `tasmota.cmd("ModBusSend …")` : c'est Tasmota en C++ qui bâtit la trame, `prepareTrame` n'est appelée que pour TCP/UDP (`:563-564`) et côté esclave (`:609`). Le sondage 0x03 fonctionnait déjà. J'avais pourtant lu ces trois lignes deux tours plus tôt — j'ai raisonné sur « la fonction existe donc elle sert » au lieu de suivre le chemin d'exécution réel. | **Avant de déclarer qu'un manque est bloquant, suivre le chemin d'exécution du cas précis**, pas la présence d'une fonction dans le fichier. Un `grep` des appelants (`prepareTrame(` → 3 sites, dont aucun sur le maître série) suffisait et coûtait 10 secondes. Corollaire : une affirmation de blocage réordonne un plan entier — elle mérite le même niveau de preuve qu'un correctif (chemin + numéro de ligne).

[2026-07-24] | J'ai repris tel quel plusieurs docs du dépôt (dont `reprise-chantier-modbus-grenier.md`) affirmant qu'`erase_upload` « efface `_persist.json` et rend les modules muets », et je l'ai re-servi à l'utilisateur comme un danger — **sans vérifier le code**. C'est l'utilisateur, auteur du dépôt, qui a levé l'erreur : dans ce dépôt, `pio-tools/post_esp32.py:385-392` joint `littlefs.bin` au flash lors d'un upload esptool, et `copy_fs_image` (`pre_utilitaires_platformio.py:500`) construit ce FS avec le `_persist.json` du device pour les cibles `buildfs`/`uploadfs`/`upload`/`erase_upload`. La commande **remplace** donc le persist de la puce par celui du dépôt ; le risque réel est un **dépôt en retard sur l'état vivant** (réglages web non reportés, rollback git), pas un effacement sec. | **Une affirmation reprise d'une doc du dépôt sur le comportement d'un outil de build se vérifie dans le code avant d'être re-servie ou recopiée ailleurs — surtout avant d'entrer dans une autre doc.** Même piège que les leçons 40/46/50 : chemin + numéro de ligne, ou rien. La preuve tenait en deux fichiers. La même formulation périmée subsiste dans `reprise-solidification-berry.md:28` et `docs/superpowers/plans/2026-07-15-solidification-berry-phase1.md:359` (+ la skill `tasmota-berry`, hors dépôt) — à aligner.

[2026-07-24] | La session precedente a ete coupee net (limite de session) juste apres 6 `Edit` sur les 3 `autoexec.be` garage : les fichiers etaient ecrits mais **jamais relus**, le `.ini` valide n'etait pas corrige, et rien n'etait commite depuis `01e3f0c9e` — 8 fichiers modifies en attente. Une reprise a ete necessaire pour reconstituer l'etat depuis l'export. | **Ne pas accumuler une longue serie d'ecritures avant le premier commit.** Des qu'un lot coherent est termine et verifie (ici : les 4 alignements doc), commiter — le lot suivant repart d'un point de retour propre. Corollaire : **relire ce qu'on vient d'ecrire dans le meme tour**, pas au tour suivant (regle 7 : prouver ce qui a rendu vert).

[2026-07-24] | J'ai monte la phase 0 (5 verrous, 16 relais virtuels, USE_MODBUS, deplacement d'un GPIO) sur le module **grenier**, parce que c'est ce que prescrivait `tasks/reprise-chantier-modbus-grenier.md`. L'utilisateur m'a arrete : le travail ModBus porte sur les **3 modules garage** (P4 maitre, cuve, rideau). 332 lignes ecrites au mauvais endroit, annulees. Le doc de reprise etait ma seule source et je ne l'ai pas confronte a l'utilisateur avant d'ecrire. | **Un document de reprise decrit l'intention passee, pas forcement la cible du jour.** Avant d'engager des ecritures sur un module que le doc designe mais que la conversation n'a pas nomme, confirmer la cible en une phrase. Corollaire verifie ici : le plan lui-meme peut ne pas transposer — sa conclusion « la phase 1 n'est pas bloquante » tenait au fait que le grenier est maitre **serie pur** ; sur le garage (`typeComm.TCP = ON`), `prepareTrame` est sur le chemin d'execution et la phase 1 devient bloquante. Verifier les `_persist.json` des modules **cibles** avant de reprendre une conclusion du plan.

[2026-07-24] | J'ai ecrit que « le P4 est maitre et a un role esclave cote cuve/rideau », sans l'avoir verifie. Faux : en serie, le code esclave est garde par `id > 0` (`controleModbus.be:303`) et le P4 est `id = 0`. Le vrai point est different et je ne l'ai trouve qu'en verifiant : `lireMsgModbus` n'est pas purement esclave — le maitre l'utilise aussi par la voie TCP (`tcpFonctions.be:199`, appel non garde par le role), ce qui invalide la frontiere du decoupage de la phase 3. | **Ne pas qualifier le role d'un module de memoire : lire la garde.** Une affirmation sur « qui execute quoi » se prouve par la condition qui entoure l'appel, pas par le nom de la fonction. Meme famille que la regle 11.

[2026-07-24] | En verifiant la syntaxe des drivers non solidifiables, j'ai lu le traceback de Berry (`:76: in function sort` / `:146: in function main`), constate que `sort` est appele AVANT `parse_file` dans `solidify_all_python.be`, et conclu que le parsing n'avait jamais eu lieu - donc que ma verification etait invalide. Fausse alerte : j'avais filtre la sortie au grep et perdu les deux lignes qui tranchaient, `Parsing: <fichier>` et le vrai message d'erreur. La sortie complete montre que `parse_file` imprime `Parsing:` (`:98`), puis `compile(src)` (`:103`), puis `compiled()` (`:104`) : une `attribute_error` sur un global nil prouve donc que la COMPILATION a reussi (piege 2). Le traceback affichait des frames externes sans rapport avec le site reel de l'erreur. | **Ne pas diagnostiquer sur une sortie filtree.** Un `grep` sur un message d'erreur retire justement le contexte qui le qualifie ; relire la sortie ENTIERE avant de conclure. Corollaire : en Berry, le traceback d'une erreur survenue dans un chunk execute par `compiled()` montre les frames de l'appelant, pas le site fautif - c'est le message (`syntax_error` vs `attribute_error`/`type_error`) qui distingue une faute de syntaxe d'un echec d'execution, pas les numeros de ligne.

[2026-07-24] | Avant de flasher, j'ai monte un banc de test Berry sur PC (`outils_docs/scripts_python/banc_test_modbus.{be,py}`, lance par le `berry.exe` natif du depot) chargeant le VRAI `modbusFonctions.be` sous globaux bouchonnes. Il a immediatement trouve un bug que la lecture seule n'avait pas tranche : `prepareTrame` emet une quantite DOUBLEE pour les trames de lecture (`nbRegistres *= 2`, :1370, sur une variable partagee reutilisee comme quantite en 0x03/0x04). crc16modbus (3 vecteurs doc) et apparieReponse (4 cas) etaient corrects. | **Un banc de logique deterministe sur PC coute ~30 min et attrape des bugs de trame AVANT un flash de 2h.** `berry.exe` compile en mode strict : il force a declarer tous les globaux du firmware (tasmota, drivers, serveur...), ce qui ISOLE proprement la logique pure. Vecteurs de reference obligatoires : ici les CRC de PROTOCOLE_MODBUS.md §8. Un bug connu se marque en echec ATTENDU (xfail) pour garder le banc vert et alerter le jour de sa correction. Ne PAS lancer ce banc ni solidifie_et_compile pendant un build de l'utilisateur : ils ecrivent dans lib/libesp32/berry_custom (le banc, lui, n'y touche pas - il ne lit que data/fs).

## Règles actives (résumé à relire en début de session)

1. Chercher le code de l'utilisateur (`git status`, diff vs upstream, `find *.be`) **avant** de décrire le dépôt.
2. Lister une arborescence avant de qualifier un dossier ; ne pas extrapoler depuis un seul fichier.
3. Encoder les conventions maison, pas la doc publique.
4. Vérifier le site d'usage avant de déclarer une coquille bugguée.
5. Relire soi-même toute conclusion de sous-agent qui devient une règle.
6. Verrou de fichier sous Windows : **nommer** le détenteur via le Restart Manager, jamais le supposer.
7. **Tout test vert doit prouver quelle ligne l'a rendu vert**, et s'accompagner d'un **témoin** (un cas qui doit échouer). Vaut pour les courses (témoin entrelacé), les exclusions, les filtres et les gardes. Piège tombé 2× le 2026-07-15.
8. Réglage VS Code : identifier le **profil actif** avant d'éditer, et lire le `package.json` de l'extension installée quand un réglage semble inopérant.
9. **Ne jamais dégrader le code de l'utilisateur pour un symptôme que seul mon outillage provoque.** Vérifier d'abord que le problème existe dans SON workflow. Pour surveiller un build en redirigeant la sortie : `PYTHONIOENCODING=utf-8 pio run ... > log` (sinon Python passe en cp1252 et `✔` tue le thread de recopie de pio, ce qui fait perdre TOUTE la sortie).
10. Poursuivre l'observation qui **contredit** l'explication en cours : c'est elle qui tranche.
11. **Un manque n'est bloquant que si le chemin d'exécution réel le traverse.** Avant de l'annoncer, `grep` les appelants de la fonction en cause — la présence d'un trou dans un fichier ne prouve pas qu'il est sur le trajet.

[2026-07-24] | Pour verifier la synchro du fork au demarrage de session, j'ai propose un hook `SessionStart` qui **lance** `synchronise_fork_tasmota.py` quand le fork a du retard. Foireux : un hook a un timeout (60 s), un merge tue en plein vol laisse un `MERGE_HEAD` et un index a moitie ecrit — l'etat conflictuel que le mecanisme etait cense eviter, cree sans temoin. Et le script demande confirmation avant de pousser (`input()`, ligne 434) : dans un hook, stdin n'est pas un terminal -> `EOFError` APRES le merge. L'utilisateur a tranche : detecteur seul. | **Un hook detecte, il n'agit pas.** Ne jamais mettre dans un hook une operation (a) non bornee en duree, ou (b) ecrite pour un humain devant un terminal (confirmation, resolution de conflit). Verifier `input(`/`isatty` dans tout script qu'on envisage d'automatiser. Corollaire ModBus/git : contre les conflits, la vraie prevention est la **frequence** (petits paquets, sur `development` propre, avant d'ouvrir un chantier), pas l'automatisme.

[2026-09-22] | J'ai ecrit un message de commit multiligne avec une syntaxe de here-string **PowerShell** (`@'...'@`) en la passant a l'outil **Bash**. Git Bash ne connait pas cette construction : le `@` d'ouverture est devenu le **titre** du commit et le `'@` de fermeture une derniere ligne de corps. Le contenu, lui, etait intact — l'erreur ne portait que sur les delimiteurs, donc invisible tant qu'on ne relit pas le message. Repris par `--amend` (commit non pousse ; un nouveau commit n'aurait pas repare un titre). | **Les deux shells coexistent sur ce poste et chacun a sa syntaxe de chaine multiligne** : outil Bash -> heredoc `<<'EOF' ... EOF` ; outil PowerShell -> here-string `@'...'@`, `'@` en colonne 0. Avant d'ecrire un message multiligne, verifier **a quel outil on parle**, pas seulement ce qu'on veut dire. Controle a cout nul apres tout commit redige ainsi : `git log -1 --format=%B | sed -n '1p;$p'` — la premiere et la derniere ligne suffisent a faire apparaitre un delimiteur avale. Meme famille que la regle 7 : ne pas supposer qu'une commande a fait ce qu'on croit, le verifier sur sa sortie.
